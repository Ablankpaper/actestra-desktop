# P8.2 产品修复针对性功能验收记录

日期：2026-08-25

验收 worktree：`/Users/zizimutou/actestra-worktrees/p8-product-fixes`

验收分支：`codex/p8-product-fixes`

验收代码 HEAD：`65957820f789028fe44cc9bfe2a701d4547b8d2e`

实现代码基线：`d07e824ce0b2dae2c72ef4a2bbbc391b5a2e75b7`

候选应用：`/Users/zizimutou/Library/Caches/Actestra/candidates/d07e824.thASb1/mac-arm64/Actestra.app`

## 验收范围和判定规则

本记录只覆盖本批次报告的五类产品问题：消息即时投影/流式与 Finish、General v1 文件能力拒绝、Team 输入校验映射、Artifact 目标工作区安全拒绝与持久化、暂停恢复后的失败分类。每个案例记录输入、预期、实际结果和可复核证据。

“通过”表示产品行为和错误分类符合契约；暂停恢复案例中的模型拒绝不是要求模型必须成功，而是要求系统保留并准确显示运行时失败，不能把它误报成恢复链路成功或泛化错误。

本次验收没有读取、记录或提交 Provider 密钥。Renderer 权限边界、`foundation/` 冻结约束和 P8.3/P8.4 的发布门禁不在本记录中宣称完成。

## 案例结果总表

| 编号 | 功能案例 | 输入/前置条件 | 预期 | 实际结果 | 判定 |
| --- | --- | --- | --- | --- | --- |
| P8-F01 | 普通 Provider 对话、即时显示、流式输出、Finish | `P8.2验收：请只回复“STREAM_OK”。` | 用户消息立即出现；Assistant 在 Finish 前逐步出现；持久化终态后 Processing 清除 | 用户消息即时出现；收到 `STREAM_OK` 前已有可见输出；Finish 后 Processing 消失，日志同时记录首个可见输出和终态持久化 | 通过 |
| P8-F02 | General v1 文件读取能力拒绝 | Team `team-7282707d325c29337fa1a0416a21220caa6a22bab2e8c01cbcfbc40ad67aceb5`；`请读取 README.md 并总结内容。` | 执行前拒绝；不出现 Glob、文件工具或网络工具 | UI 显示 “General needs source text that was not provided...” 指引；General Failed，Team Blocked；没有 Glob/文件/网络工具，也未授予文件能力 | 通过 |
| P8-F03 | 合法 LF 多行任务 | Team `team-dc98ce62a5df120cc468943e0b66ce3fa8e884c7cfeeac666ae4fb0265d8460f`；`第一行` + LF + `第二行` | 普通 LF 是合法正文，不应被误判为 invalid request | Team 接受并启动；随后由验收人员手动取消；run `team-run-aa1dabb3cabc3ba9ac9c319609abf868bab6c59449b6ba83c25194b62ddb25ef` 最终为 `cancelled` | 通过 |
| P8-F04 | 非法控制字符错误映射 | Team `team-2dd1d1cb933eee6ca9d8482a3f2037a999078a5ec88c1fb91c7964e49bf1308a`；`合法前缀\\u0000非法`（实际 NUL） | 不创建 run、不调用 Planner/Worker；返回明确的 `team-invalid-request` 用户提示 | UI 显示 “This Team task body has an invalid format. Remove control characters and leading or trailing blank lines...” ；数据库中该 Team 没有 run | 通过 |
| P8-F05 | dirty 工作区 Artifact 交付 | Artifact `artifact-coding-11aee0960c9d861d364cd06c408a3517a8501197fa8f54eee0fb99da900fd992`；目标工作区带未提交改动 | 安全拒绝并写入 durable `workspace-dirty`；不保持 pending，不修改工作区 | `artifact_deliveries.state = failed`，`failure_code = workspace-dirty`，消息为 `Workspace has uncommitted changes` | 通过 |
| P8-F06 | HEAD 漂移 Artifact 交付 | Artifact `artifact-coding-d963c543a751b5ec1603e088450597a158371661e48bb8268a998ef26e6bccc8`；创建 Artifact 后目标 HEAD 移动 | 安全拒绝并写入 durable `head-drift`；不应用 patch | `artifact_deliveries.state = failed`，`failure_code = head-drift`，消息为 `Workspace HEAD has moved since patch was created`；目标未被修改 | 通过 |
| P8-F07 | Coding Artifact 明确批准后应用 | Artifact `artifact-coding-7ab094fb74e65077b21d0be05f9467fd12c564f90c44f50e2acad48a51942f28`；干净目标、HEAD 未漂移、用户完成批准 | 仅在批准后应用；持久化为 applied；UI 显示变更数并禁用重复 Apply | `state = applied`，`changed_file_count = 1`，`verified_head = 222b98d4dc64fab08c61808b3e592df93e2c97e5`；UI 显示 `Applied 1 changed file(s)`，Apply 已禁用 | 通过 |
| P8-F08 | 暂停 → 恢复 → 失败分类 | Team `team-b1523c74616319d6c1d63f1892048da97aebb97e907aa2c951d902dd2773647a`；`Generate a concise three-part status report with one sentence per part, then wait for the user control action.` | 暂停边界持久化；恢复后不得覆盖真实失败原因；若模型拒绝，应显示明确运行时 code | run `team-run-964700f5fd1cadf46b7dad3fdde5f5c158a06ec0d30834f940ecdad41b97066f`：revision 6 Goose `Paused`/Team `Blocked`，恢复后 revision 7 Goose `Failed`；UI 和 DB 均为 `model-completion-refused`，无残留 Worker | 通过（恢复链路及分类通过；模型本身拒绝是被正确保留的结果） |

## 逐案证据

### P8-F01：即时消息、流式输出和 Finish

- 真实候选进程：Actestra 主进程 PID `33804`，AionCore PID `33906`（验收期间采样）。
- AionCore 日志 `/Users/zizimutou/Library/Application Support/Actestra/profiles/v1/default/logs/2026/08/25/2026-08-25.aioncore.log`：
  - `User message persisted`（conversation `a92c74eb`，约第 3526 行）；
  - `feedback.runtime.turn_first_visible_output`（turn `turn_302fcc53`，约第 3555 行）；
  - `feedback.runtime.turn_terminal` / `Finish`，`persisted_assistant_output: true`（约第 3557 行）。
- 同一候选上的第二个长输出也在 Finish 前出现可见内容，并在终态后清除 Processing。
- 下游 DOM 测试覆盖：`useAcpMessage.dom.test.ts` 的 ACP/AionRS chunk-before-Finish、`conversationCommandQueueDrain.dom.test.tsx` 的 durable reread/旧 Finish 防护、`AcpSendBox.dom.test.tsx` 与 `AionrsSendBox.dom.test.tsx` 的 optimistic user projection。

### P8-F02：General v1 文件读取拒绝

- Team UI 实际提示：`General needs source text that was not provided. Paste the missing text into the task and retry, or choose another execution path.`
- Team run `team-run-3329e058b7f975cdb6325ad624e797b5ae866fa46595b48257a0e9dadd4afc07` 的 durable snapshot 显示 General attempt 的 `incidentCode = general-input-required`，未出现文件工具执行。
- 相关自动化断言：`classifies an explicit file-read instruction before General Worker execution`；覆盖 Main admission 在 `model-requested` 前拒绝。

### P8-F03：合法 LF 多行

- SQLite `team_runs` 查询结果：run `team-run-aa1dabb3cabc3ba9ac9c319609abf868bab6c59449b6ba83c25194b62ddb25ef`，revision `6`，status `cancelled`。
- 该状态来自验收人员主动取消，不是输入校验失败；因此不能把普通 LF 改写成 `team-invalid-request`。
- 相关自动化断言：`tests/compatibility/aionuiTeamBridgeMultiline.test.ts` 的 `accepts multiline content with LF`，以及 Team bridge 多行活动投影测试。

### P8-F04：非法控制字符

- UI 原文：`This Team task body has an invalid format. Remove control characters and leading or trailing blank lines, then submit the task again.`
- SQLite 查询确认该 Team 没有对应 `team_runs` 记录，Planner/Worker 未启动。
- 下游 DOM 断言：`shows only the dedicated Team invalid-request error and clears the failed optimistic send`；Root 侧映射测试覆盖 `team-invalid-request`。

### P8-F05/P8-F06/P8-F07：Artifact durable 交付边界

验收使用 Main-owned SQLite：`/Users/zizimutou/Library/Application Support/Actestra/profiles/v1/default/state/actestra.sqlite3`。以下是对 `artifact_deliveries` 的直接查询结果摘要：

```text
artifact-coding-11aee096...  failed   workspace-dirty  Workspace has uncommitted changes
artifact-coding-d963c543...  failed   head-drift      Workspace HEAD has moved since patch was created
artifact-coding-7ab094fb...  applied  (null)          changed_file_count=1, verified_head=222b98d4...
```

Root 测试覆盖 `projects a restart-durable workspace-dirty delivery`、`projects a restart-durable head-drift delivery`、`preserves the exact workspace-dirty preflight rejection` 和 `preserves the exact head-drift preflight rejection`；成功应用路径还验证批准、SHA、目标 HEAD 和 UI 禁用重复应用。

### P8-F08：暂停恢复与运行时失败分类

- run `team-run-964700f5fd1cadf46b7dad3fdde5f5c158a06ec0d30834f940ecdad41b97066f` 的 `team_run_revisions` 为 1–7，时间连续记录暂停和恢复边界。
- revision 6：Goose node `paused`，`blocked_reason = paused`，说明为用户从 Team 页面请求暂停。
- revision 7：Goose node `failed`，attempt `status = failed`，`incidentCode = model-completion-refused`；UI 显示 `The bounded Worker attempt failed: model-completion-refused.`
- General node 仍为 completed，Team durable status 为 blocked，恢复逻辑没有把运行时失败覆盖成成功或新的泛化错误。
- 采样进程树没有 Goose/General Worker 残留。
- 自动化断言：`holds a completed journey at the persisted pause boundary until resume`、`preserves a General model failure that lands while the Team attempt is paused`。

## 自动化验收命令与结果

### Root 针对性集合

```text
bun run test -- tests/renderer/conversationTerminal.test.ts tests/renderer/optimisticUserMessage.test.ts \
  tests/main/aionuiTeamService.test.ts tests/main/teamJourneyWorkerRouter.test.ts \
  tests/main/aionuiGeneralWorkJourneyService.test.ts tests/main/aionuiCodingArtifactService.test.ts \
  tests/main/aionuiCodingJourneyBridgeService.test.ts tests/compatibility/aionuiTeamBridge.test.ts \
  tests/compatibility/aionuiTeamBridgeMultiline.test.ts tests/scripts/actestraTeamWorkerReadiness.test.mjs \
  tests/scripts/p8ProductAcceptancePatch.test.mjs

Test Files  10 passed (10)
Tests       189 passed (189)
```

### Materialized downstream Renderer 集合

先执行仓库规定的 `bun run downstream:aionui:materialize`，再执行 `bun install --cwd .actestra/aionui-v2.1.41 --frozen-lockfile`（第一次运行因 materialized tree 缺少 `eventemitter3`、`@arco-design/web-react`、`swr` 而未进入测试；这是环境前置问题，不是产品断言失败）。安装成功后原命令重新执行：

```text
bun run --cwd .actestra/aionui-v2.1.41 test -- \
  tests/unit/renderer/useAcpMessage.dom.test.ts \
  tests/unit/conversation/runtime/conversationCommandQueueDrain.dom.test.tsx \
  tests/unit/renderer/conversation/AcpSendBox.dom.test.tsx \
  tests/unit/renderer/conversation/AionrsSendBox.dom.test.tsx \
  tests/unit/renderer/team/TeamChatView.dom.test.tsx \
  tests/unit/renderer/team/ActestraTeamWorkspace.dom.test.tsx

Test Files  6 passed (6)
Tests       77 passed (77)
```

## 结论与边界

8 个针对性案例均通过。验收期间唯一的测试启动失败是 downstream 依赖未安装，已按锁文件完成安装并重新验证为 77/77；没有因产品行为失败而修改运行代码。

本记录证明的是 `codex/p8-product-fixes` 上的 P8.2 产品修复在 macOS 候选和自动化边界的验收结果。PR #71/#72 尚未在本记录创建时合并；P8.3 候选完整性/签名/更新回滚和 P8.4 清洁机器安装升级卸载、真实 Provider 内部验收、runbook/issue intake 仍是独立门禁，不能由本记录替代。
